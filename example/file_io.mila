
// open the file (this file)
var f = open("file_io.mila", "r+");

// go to the end
fseek(f, 0, SEEK_END);
// get the length
var size = ftell(f);

// go to beginning
fseek(f, 0, SEEK_SET);

// read the entire file
var text = fread(f, size);

// print it
println(text);

// go back to the beginning
fseek(f, 0, SEEK_SET);

// overerite file
fprint(f, text + "\n// comment!");

// close the file
fclose(f);