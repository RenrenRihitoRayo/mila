namespace test {
    set .greet = fn(name) {
        println("Hello, " + name + "!");
    };
}

var d = dict(
    "name", "Darren",
    "age",  17
);

println(d["name"]);
var d["name"] = "Edward";
println(d["name"]);