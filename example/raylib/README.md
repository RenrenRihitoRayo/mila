# This directory is a test for raylib!

Yes it works with most functions given that it doesnt
use function pointers (needs to be casted to void* which
loses type checking fidelity) and variadic functions.