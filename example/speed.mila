var start = get_time();

var i = 1000000u;

while (i > 0u) {
    set tmp = i * i * i;
    // this is a test comment
    set i = i - 1u;
}

var end = get_time() - start;
println("Time: " + end + "s");

