set a = list();

var fact = fn(n) {
    var s = 1u;
    while (n > 1)
    {
        set s *= n;
        set n -= 1u;
    }
    return s;
}

foreach item : range(1, 1001) {
    list.append(a, fact(cast.i2u(item)));
}

printf("%s", "yes");

println(a);