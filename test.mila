load("./hello.so");
hello();

let loop = 0;
while (loop < 100) {
	if ( loop % 3 == 0 && loop % 5 == 0 ) {
		println("fizzbuzz");
	} elif ( loop % 3 == 0 ) {
		println("fizz");
	} elif ( loop % 5 == 0 ) {
		println("buzz");
	} else {
		println(loop);
	}
	let loop = loop + 1;
}